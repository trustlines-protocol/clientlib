This folder contains the file `addresses.json` created when running the e2e test locally.
The file contains the identity implementation and proxy factory addresses as they are deployed by `tldeploy`.
They are the addresses that have to be used to deploy an identity in the e2e tests.
